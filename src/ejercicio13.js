let operador = "claro"
let minutos = 75
let multiplicacionTigo= minutos*200
let multiplicacionClaro= minutos*100
let multiplicacionMovistar= minutos*250
if (operador==="tigo"){
    console.log(`Su total a pagar es= ${multiplicacionTigo}`)
}else if (operador==="claro"){
    console.log(`Su total a pagar es= ${multiplicacionClaro}`)
}else if (operador==="movistar"){
    console.log(`Su total a pagar es= ${multiplicacionMovistar}`)
}